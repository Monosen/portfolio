<footer>
  <div class="container">
    <div class="footer-center">
      <div class="text-align-center">
        <h3 class="text-rgb-two text-size">
          De vez en cuando acepto oportunidades como autónomo.
        </h3>
        <p class="footer-text">
          ¿Tiene un proyecto interesante en el que necesita ayuda?
          <span
            >Envíame un mensaje y
            <a class="footer-link" href="./contact.html"
              ><span>charlemos.</span>
            </a>
          </span>
        </p>
      </div>
    </div>
  </div>
</footer>

<style>
  :root {
    --black: #000000;
    --white: #ffffff;
    --fondo: #111;
    --fondoTwo: #191919;
    --pink: #ffc4ff;

    --fuenteOne: "Bogart-Medium", "Roboto", sans-serif;
    --fuenteTwo: "Biotif-Medium", sans-serif;
  }

  /* Glovales */

  @font-face {
    font-family: Bogart-Medium;
    src: url("../../../assets/fonts/Bogart-Medium-trial.ttf") format("truetype");
  }

  @font-face {
    font-family: Biotif-Medium;
    src: url("../../../assets/fonts/Biotif-Medium.ttf") format("truetype");
  }

  html {
    font-size: 62.5%;
    box-sizing: border-box;
  }

  *,
  *:before,
  *:after {
    box-sizing: inherit;
  }

  body,
  html {
    font-family: var(--fuenteOne);
    background-color: var(--fondo);
  }

  ul {
    list-style: none;
    margin: 0;
    padding: 0;
  }

  a {
    text-decoration: none;
  }

  i {
    color: var(--white);
  }

  h1,
  h2,
  h3,
  h4,
  h5 {
    font-family: var(--fuenteOne);
    font-weight: 500;
    color: var(--white);
  }

  a,
  button,
  h6,
  input,
  li,
  p {
    font-family: var(--fuenteTwo);
    color: var(--white);
  }

  .container {
    max-width: 90%;
    margin: auto;
  }

  @media (min-width: 1200px) {
    .container {
      max-width: 80%;
    }
  }

  .footer-center {
    margin: auto;
    padding: 25rem 0;
    max-width: 700px;
  }

  .text-align-center {
    text-align: center;
  }

  .text-rgb,
  .text-rgb-two,
  .text-rgb-three {
    font-size: 5.2rem;
    letter-spacing: -0.15rem;
    background-image: linear-gradient(
      271deg,
      #a0e9ff 30%,
      #a162e8 50%,
      #f093b0 70%,
      #edca85 94%
    );
    background-clip: text;
    -webkit-background-clip: text;
    color: transparent;
    margin: 0;
  }

  .text-size {
    font-size: 4rem;
  }

  @media (min-width: 576px) {
    .text-size {
      font-size: 6.5rem;
    }
  }

  .footer-text {
    font-size: 2rem;
    line-height: 160%;
    margin-top: 3rem;
  }

  .footer-link {
    position: relative;
  }

  .footer-link span::after {
    content: "";
    background-color: var(--pink);
    position: absolute;
    left: 0;
    top: calc(100% + 0.7rem);
    width: 100%;
    height: 0.4rem;
    border-radius: 1rem;
    transition: opacity 0.3s;
  }
</style>
